#!/bin/bash
# Post-build script for Binder
# This runs after the environment is created

# Enable Jupyter extensions if needed
# jupyter nbextension enable --py widgetsnbextension --sys-prefix

echo "Forge environment ready!"
